# Secrets & credentials — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
# .secrets.baseline is intentionally tracked — it's the CI reference for detect-secrets
# Only the real secrets file is excluded. The baseline contains hashes, not secrets.
!.secrets.baseline

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.Python
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
*.tar

# IDE & AI coding tools
.vscode/
.idea/
*.swp
*.swo
.DS_Store
.claude/

# Build artifacts
*.whl
*.egg
*.so
sbom.json

# Playwright E2E
e2e/.auth/
playwright-report/
test-results/

# Test fixtures with real data (corpus uses synthetic data only)
tests/fixtures/real_data/
