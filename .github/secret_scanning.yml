paths-ignore:
  # Test fixtures contain intentionally fake tokens used as detection corpus samples.
  # These are not real credentials — they are synthetic examples for the ML recall evaluator.
  - "apps/workers/tests/fixtures/**"
  - "apps/workers/tests/**"
  - "apps/api/tests/**"
