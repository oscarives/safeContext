# workers/agents — Dramatiq worker actors
# Dramatiq discovers actors by importing their modules.
# Import each sub-module explicitly to register @dramatiq.actor decorators.
# Usage: python -m dramatiq workers.agents --processes 2 --threads 4
