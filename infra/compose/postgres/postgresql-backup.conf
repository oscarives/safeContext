# WAL archiving — enables point-in-time recovery and streaming backup
wal_level = replica
archive_mode = on
archive_command = 'cp %p /backups/wal/%f'
archive_timeout = 300        # archive WAL at most every 5 minutes
max_wal_senders = 3
wal_keep_size = 128          # keep 128MB of WAL segments
